// Generate a random Ts code here.

let randomNumber = Math.random() * 10;
let randomColor = `rgb(${Math.floor(randomNumber)}, ${Math.floor(randomNumber)}, ${Math.floor(randomNumber)})`;
document.body.style.backgroundColor = randomColor;
